/// @desc Quest script for the Odin quest system
/// @arg event

// Grab the requested event to execute
var _event = argument0;

// Switch through all available events
switch (_event) {
	case "start":
		
		break;
}